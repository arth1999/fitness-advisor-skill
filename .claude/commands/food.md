---
description: 饮食营养建议（标准长度）
argument-hint: <message>
---
读 `commands/food.md` 获取模块指示。
按 "default" 长度变体处理用户请求："$ARGUMENTS"。
先读 `commands/_shared/length-rules.md`, `commands/_shared/safety-footer.md`, `commands/_shared/data-loading.md` 获取共享规则。

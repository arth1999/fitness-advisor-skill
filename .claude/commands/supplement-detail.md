---
description: 补剂证据评估（详细模式）
argument-hint: <message>
---
读 `commands/supplement.md` 获取模块指示。
按 "detail" 长度变体处理用户请求："$ARGUMENTS"。
先读 `commands/_shared/length-rules.md`, `commands/_shared/safety-footer.md` 获取共享规则。
